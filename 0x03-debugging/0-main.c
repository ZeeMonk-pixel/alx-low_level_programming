#include "main.h"

/**
 * main(void)- test that the function positive_or_negative()
 *  gives the correct output when given a case of 0
 *  Return: returns 0
 */

int main(void)
{
	int i = 0;

	positive_or_negative(i);
	return (0);
}
